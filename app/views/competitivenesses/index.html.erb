<p id="notice"><%= notice %></p>

<h1>Sobre a competitividade e o ambiente de negócios</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Questão</th>
    <th>Resposta</th>
  </tr>
  </thead>

  <tbody>
  <% @competitivenesses.each do |competitiveness| %>
    <tr>
      <td><%= competitiveness.descripition %></td>
      <td>
        <% @answer.each do |resposta| %>
          <%= resposta.question_id == competitiveness.id ? resposta.answer : "" %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ver\Editar respostas', answers_path %>
