

<h1>Sustentabilidade financeira</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Questão</th>
    <th>Resposta</th>
  </tr>
  </thead>

  <tbody>
  <% @finances.each do |finance| %>
    <tr>
      <td><%= finance.descripition %></td>
      <td>
        <% @answer.each do |resposta| %>
          <%= resposta.question_id == finance.id ? resposta.answer : "" %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ver\Editar respostas', answers_path %>