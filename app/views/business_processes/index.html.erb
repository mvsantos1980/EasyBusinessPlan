<p id="notice"><%= notice %></p>

<h1>Sobre os processos do negócio</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Questão</th>
    <th>Resposta</th>
  </tr>
  </thead>

  <tbody>
  <% @business_processes.each do |business| %>
    <tr>
      <td><%= business.descripition %></td>
      <td>
        <% @answer.each do |resposta| %>
          <%= resposta.question_id == business.id ? resposta.answer : "" %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ver\Editar respostas', answers_path %>
