<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'Business Plan Easy', root_path, class: 'navbar-brand'  %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <%= link_to 'Editar perfil', edit_user_registration_path, class: 'btn btn-secondary' %> |
        <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: 'btn btn-secondary'  %>
      <% else %>
        <%= nav_link 'Cadastrar', new_user_registration_path %>
        <%= nav_link 'Entrar', new_user_session_path %>
      <% end %>
    </ul>
  </div>
</nav>