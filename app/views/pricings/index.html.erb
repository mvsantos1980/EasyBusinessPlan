

<h1>Precificação</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Questão</th>
    <th>Resposta</th>
  </tr>
  </thead>

  <tbody>
  <% @pricings.each do |pricing| %>
    <tr>
      <td><%= pricing.descripition %></td>
      <td>
        <% @answer.each do |resposta| %>
          <%= resposta.question_id == pricing.id ? resposta.answer : "" %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ver\Editar respostas', answers_path %>
